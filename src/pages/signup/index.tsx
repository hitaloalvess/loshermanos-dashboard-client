import { AuthContainer, FormSignUp } from '../../components';

export default function SignUp() {
    return (
        <AuthContainer sideImage="right" imageSrc="/images/bannerSignup.svg">
            <FormSignUp />
        </AuthContainer>
    );
}
