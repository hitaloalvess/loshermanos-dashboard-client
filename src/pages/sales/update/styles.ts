import styled from 'styled-components';

const ContentUpdateSale = styled.main`
    display: flex;
    flex-direction: column;
    gap: 4.4rem;
`;

const ContentUpdateSaleHeader = styled.header`
    display: flex;
    flex-direction: column;
    gap: 2.4rem 0;

    h1 {
        font-size: 3.6rem;
        font-weight: bold;
    }
`;

export { ContentUpdateSale, ContentUpdateSaleHeader };
