import styled from 'styled-components';

const ContentRegisterSale = styled.main``;

const ContentRegisterSaleHeader = styled.header`
    display: flex;
    flex-direction: column;
    gap: 2.4rem 0;
    h1 {
        font-size: 3.6rem;
        font-weight: bold;
    }
`;

export { ContentRegisterSale, ContentRegisterSaleHeader };
